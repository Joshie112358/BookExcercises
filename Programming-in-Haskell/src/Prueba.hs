module Prueba where

import System.IO
import System.Random

nums :: [Int]
nums = []

-- randomarray :: Int -> IO [Int]
-- --randomarray n = sequence [x | x <- randomRIO (1,n), x /= init randomarray ]
-- randomarray n = do
--   let x = [1..n]
  


-- switch :: Int -> Int -> [Int] -> [Int]
-- switch x y ns = (take (x-1) ns) : (ns !! y-1) :


-- este es un cambio que quiero ver reflejado :v
-- porfa dejame ver el cambio
-- jhriuahgiurhiuaruih
